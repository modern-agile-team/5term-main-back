services:
  nest:
    image: ghcr.io/j-zzi/nest:latest
    container_name: nest-container
    ports:
      - 3000:3000
    env_file:
      - ~/app/config/default.yml
      - ~/app/config/development.yml
      - ~/app/config/production.yml
    restart: always
